[pytest]
DJANGO_SETTINGS_MODULE = config.settings

python_files =
    test_*.py
    *_test.py

python_classes = Test*

python_functions = test_*

testpaths =
    apps
    tests

addopts =
    --reuse-db
    --strict-markers
    --tb=short

filterwarnings =
    ignore::DeprecationWarning
